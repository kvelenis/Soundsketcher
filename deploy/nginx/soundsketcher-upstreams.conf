# Copy or symlink this file into nginx's http context, for example:
#   /etc/nginx/conf.d/soundsketcher-upstreams.conf
#
# The matching location blocks live in:
#   deploy/nginx/soundsketcher-locations.conf

upstream soundsketcher_legacy {
    server 127.0.0.1:5002;
}

upstream soundsketcher_refactor {
    server 127.0.0.1:5013;
}
